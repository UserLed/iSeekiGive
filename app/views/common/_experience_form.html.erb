<%= form_for e, :remote => true, :html => {:class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'text_field', :required => true %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :company_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :company_name, :class => 'text_field', :required => true %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :start_date, "Time Period", :class => 'control-label' %>
    <div class="controls">
      <%= f.date_select :start_date, {:start_year => Time.now.year - 50, :end_year => Time.now.year, :discard_day => true}, {:class => 'date-select'} %>  to
      <%= f.date_select :end_date, {:start_year => Time.now.year - 50, :end_year => Time.now.year + 50, :discard_day => true}, {:class => 'date-select'} %>
    </div>
  </div>
  <div class="control-group">
    <div class="controls">
      <%= f.submit "Done", :class => "btn btn-medium btn-primary" %>
      <%= link_to "Cancel", 'javascript:void(0)',:class => "btn btn-info cancel"  %>
    </div>
  </div>
<% end %>