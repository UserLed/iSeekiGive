<%= form_for(@billing_setting) do |f| %>
  <% if @billing_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billing_setting.errors.count, "error") %> prohibited this billing_setting from being saved:</h2>

      <ul>
      <% @billing_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
